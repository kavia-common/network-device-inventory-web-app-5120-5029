# PUBLIC_INTERFACE
# Vite environment variables for ReactFrontend

# Base URL for Flask backend API
# In local dev with backend on 3001 (container), you can point to http://localhost:3001
# If using backend default 5000 locally, set http://localhost:5000
VITE_API_BASE_URL=http://localhost:3001

# Port for Vite dev server and preview. The platform expects 3000.
VITE_PORT=3000
