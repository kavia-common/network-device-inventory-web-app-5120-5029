# Port for the Vite dev server and preview server.
# If 3000 is busy in your environment, change this value (e.g., 3002).
VITE_PORT=3000

# HMR websocket client port. In most preview environments, this should match the public port.
# If your reverse proxy maps a different external port, set that here.
VITE_HMR_CLIENT_PORT=3000

# Backend API base URL used by the frontend to call the Flask API.
# Defaults to http://localhost:3001 if unset.
# Example for local dev:
# VITE_API_BASE_URL=http://localhost:3001
