# Vite environment variables for ReactFrontend
# Copy this file to `.env` and adjust as needed.

# Backend API base URL used by the frontend to call the Flask API.
# Example for local dev (Flask runs on port 5000 by default):
VITE_API_BASE_URL=http://localhost:5000

# Optional extras you can set later if needed:
# VITE_LOG_LEVEL=info
# VITE_BUILD_ENV=development
