# PUBLIC_INTERFACE
# Environment configuration for the React Frontend
# Copy this file to .env and adjust values per environment.

# Base URL of the Flask Backend API (no trailing slash).
# Defaults to http://localhost:3001 if not set.
REACT_APP_API_BASE_URL=http://localhost:3001

# Optional API key sent as X-API-KEY header with every request.
# Leave empty if backend does not require API key.
REACT_APP_API_KEY=
