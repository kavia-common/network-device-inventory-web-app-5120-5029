# Example environment for ReactFrontend (Vite)
# Copy to ".env" and adjust as needed.

# Backend API base URL used by the frontend for requests.
# Default backend runs on http://localhost:5000
VITE_API_BASE_URL=http://localhost:5000

# Optional: override port used by Vite dev server and preview.
# Platform expects frontend on port 3000.
VITE_PORT=3000
