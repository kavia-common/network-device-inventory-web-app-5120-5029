# PUBLIC_INTERFACE
# Example environment variables for the React frontend (Vite).
# Copy this file to ".env" and adjust as needed.

# Base URL for the Flask backend API.
# If not set, the app defaults to http://localhost:3001
VITE_API_BASE_URL=http://localhost:3001

# Port for the Vite dev server and preview.
# If not set, vite.config.ts defaults to 3000.
VITE_PORT=3000
