# React Frontend environment variables
# Copy this file to .env and adjust values as needed.

# Base URL of the Flask Backend API (default documented: http://localhost:3001)
REACT_APP_API_BASE_URL=http://localhost:3001

# Optional API key. If set, it will be sent as X-API-KEY header on every request.
REACT_APP_API_KEY=

# Notes:
# - React dev server runs on port 3000 by default.
# - Start order: MongoDB (5001) -> Flask Backend (3001) -> React Frontend (3000).
